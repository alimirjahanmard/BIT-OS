#bitos it's os for run on esp32(micropython)
#this file it's kernel
import os
import time
import UI
import APP_API
#this importion fo app
UI.csui()
UI.wui()
UI.hui()
while True:
    UI.aui()
    APP_API.apr